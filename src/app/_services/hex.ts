export class Hex {
    x: number;
    y: number;
    value: number;
    resource: string;
    constructor(x: number, y: number, value: number, resource: string) {
        this.x = x;
        this.y = y;
        this.value = value;
        this.resource = resource;
    }
}
